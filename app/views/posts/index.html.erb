<span class="post">
<h1>Post</h1></span>
<ul>
  <% @posts.each do |p| %>
    <ol>
      <%= link_to cl_image_tag(p.image, width: 200, height: 200, crop: :fill), post_path(p.id) %>
    </ol>
  <% end %>
</ul>


